// import { getLettersCrafted } from "./dataAccess.js"

// const crafted = getLettersCrafted()


/* export const LetterCrafted = () => {
    let html = `
        <fieldset>
            <legend>Author</legend>
            <select id="authors">
                <option value="0">Please choose your author.</option>
                ${
                    crafted.map(
                        (author) => {
                            `<option value="${author.id}">${author.name}</option>`
                        }
                    ).join("")
                }
            </select>
        </fieldset    `

    return html //returns html string
} */